{% extends "admin.j2" %}
{% block adminstuff %}
    <section>
        <h2>Genres in db:</h2>
        {% if genres %}
            <table>
                <tr>
                    <th>name</th>
                </tr>
                {% for g in genres %}
                <tr>
                    <td>{{ g.name }}</td>
                </tr>
                {% endfor %}
            </table>
        {% else %}
            <p>No genres yet...</p>
        {% endif %}
        <h2>Add new:</h2>
        <form action="" method="POST" novalidate>
            {{ form.hidden_tag() }}
            <p>
                {{ form.name.label }}<br>
                {{ form.name(size=32) }}
                {% for e in form.name.errors %}
                    <div style="color: red;">{{ e }}</div>
                {% endfor %}
            </p>
            <p>
                {{ form.submit() }}
            </p>
        </form>
    </section>
{% endblock adminstuff %}
